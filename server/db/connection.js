const mongoose=require("mongoose"),DB=process.env.DATABASE;mongoose.connect(DB).then((()=>{console.log("Connection with database is Successful!")})).catch((o=>{console.log("Connection with database Failed!",o)}));